package model.menuBar;

import javafx.geometry.Pos;
import javafx.scene.Scene;
import javafx.scene.control.Button;
import javafx.scene.control.TextField;
import javafx.scene.layout.HBox;
import javafx.scene.layout.VBox;
import javafx.stage.Modality;
import javafx.stage.Stage;

public class WindowsNewProject {
	public static void display(){
		/*
		Stage window = new Stage();
		window.initModality(Modality.APPLICATION_MODAL);
		window.setTitle("Nouveau");
		window.setMinWidth(250);
		
		TextField canvasWidth = new TextField();
		TextField canvasHeight = new TextField();
		
		Button create = new Button("Créer");
		
		VBox layout = new VBox(10);
		layout.getChildren().addAll(canvasWidth, canvasHeight, create);
		layout.setAlignment(Pos.CENTER);
		Scene scene = new Scene(layout);
		window.setScene(scene);
		
		window.showAndWait();
		*/
	}
}
